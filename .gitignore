rlvenv/
logs/*.log
__pycache__/
*.pyc

# LaTeX build artifacts (keep main.tex, refs.bib, main.pdf)
paper/*.aux
paper/*.log
paper/*.bbl
paper/*.blg
paper/*.out
paper/*.synctex.gz
pilot_sweep.log
pilot4_sweep.log

# The manuscript is kept locally, not published to this repo (see README):
# TNNLS review is double-anonymous (confirmed against the live IEEE CIS page),
# so the submitted PDF has no identifying info and links to an anonymized
# mirror of this repo instead of here directly. Also, a reproducibility repo
# doesn't need the manuscript itself, just the code, data, and figures.
paper/
