rlvenv/
logs/*.log
__pycache__/
*.pyc

# LaTeX build artifacts (keep main.tex, refs.bib, main.pdf)
paper/*.aux
paper/*.log
paper/*.bbl
paper/*.blg
paper/*.out
paper/*.synctex.gz
pilot_sweep.log
pilot4_sweep.log

# The manuscript is kept locally, not published to this repo (see README):
# a reproducibility repo doesn't need the paper itself, just the code, data,
# and figures it was built from. (TNNLS review is single-blind, not
# double-anonymous, so this is a scope choice, not an anonymity precaution.)
paper/
